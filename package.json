{
  "name": "micro-api",
  "version": "0.0.1",
  "description": "Minimal routing layer for building JSON APIs with Zeit's micro",
  "main": "./dist/index.js",
  "repository": {
    "url": "https://github.cim/possibilities/micro-api",
    "type": "git"
  },
  "author": "Mike Bannister <mikebannister@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "mkdir -p ./dist && async-to-gen ./index.js > ./dist/index.js",
    "test": "ava --fail-fast --verbose",
    "test:watch": "npm test -- --watch"
  },
  "dependencies": {
    "test-listen": "^1.0.0",
    "uniloc": "^0.3.0",
    "uuid": "^3.0.1"
  },
  "devDependencies": {
    "async-to-gen": "^1.3.0",
    "ava": "0.17.0",
    "micro": "^6.1.0",
    "request-promise": "^4.1.1"
  }
}
